import { StyleSheet } from 'react-native'
import { Fonts, Colors } from '../../Themes/'

export default StyleSheet.create({
  centered: {
    alignItems: 'center'
  },
  text: {
    color: '#fff'
  }
})
